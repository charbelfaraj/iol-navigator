import React from 'react'
import ReactDOM from 'react-dom/client'
import IOLNavigator from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <IOLNavigator />
  </React.StrictMode>,
)
